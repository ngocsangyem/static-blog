mixin tag(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.tag(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content