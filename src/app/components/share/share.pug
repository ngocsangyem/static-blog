mixin share(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.share(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content