mixin pagination(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.pagination(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content