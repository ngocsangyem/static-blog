mixin related-article(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.related-article(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content