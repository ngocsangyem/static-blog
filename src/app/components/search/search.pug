mixin search(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.search(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content