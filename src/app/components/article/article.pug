mixin article(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.article(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content