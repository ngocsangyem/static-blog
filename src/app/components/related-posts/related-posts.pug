mixin related-posts(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.related-posts(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content